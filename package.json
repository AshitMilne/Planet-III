{
  "name": "planet-iii",
  "version": "1.0.0",
  "description": "Publishing stack for Planet-III using Eleventy",
  "main": "index.js",
  "scripts": {
    "start": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "watch": "npx @11ty/eleventy --watch",
    "clean": "rimraf _site",
    "nightly": "npm run build && git add . && git commit -m \"Nightly commit $(date -u +\"%Y-%m-%d %H:%M:%S\")\" && git push origin main"
  },
  "author": "Ash",
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "rimraf": "^6.0.0"
  }
}
